services:
  - type: web
    name: cinder-api
    runtime: python
    buildCommand: pip install -r requirements-training.txt
    startCommand: uvicorn backend.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FRONTEND_ORIGIN
        sync: false